export enum UserRequestField {
  SANITIZED_PROMPT = 'sanitizedPrompt',
  LLM_RESPONSE = 'llmResponse',
  SANITIZED_RESPONSE = 'sanitizedResponse',
  PROMPT_MASKED_EMAILS = 'promptMaskedEmails',
  RESPONSE_MASKED_EMAILS = 'responseMaskedEmails'
}
